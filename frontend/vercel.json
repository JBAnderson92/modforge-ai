{
    "functions": {
        "package.json": {
            "runtime": "nodejs20.x"
        }
    },
    "buildCommand": "npm run build",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}